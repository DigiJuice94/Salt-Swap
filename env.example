# Optional: use your own RPC endpoints for better reliability/rate limits
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
ETH_RPC_URL=https://ethereum-rpc.publicnode.com
BNB_RPC_URL=https://bsc-dataseed.binance.org

# Optional. Salt will try GoPlus without a token; add a Bearer access token if your account/API plan requires one.
GOPLUS_ACCESS_TOKEN=

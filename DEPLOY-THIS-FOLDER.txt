THE TRENCHES V1.11.13 — DROP-IN UPDATE / NO GITHUB WIPE

YOU DO NOT NEED TO DELETE THE OLD GITHUB FILES.

1. VERCEL
   Settings -> Build and Deployment -> Root Directory
   Make sure Root Directory is COMPLETELY BLANK.
   Save.

2. GITHUB
   Open the ROOT of the Salt-Swap repository.
   Upload the CONTENTS of this update folder to the ROOT.
   Allow GitHub to replace files with matching names.
   Commit directly to main.

3. WHAT THIS UPDATE DOES
   - Replaces index.html
   - Replaces styles.css
   - Replaces api.mjs
   - Replaces vercel.json
   - Overwrites old conflicting api.js / health.js / scan.js / app.js /
     index.js / main.jsx / vite.config.js / package.json with harmless
     neutralized versions.
   - Old ZIPs and old image files may remain; Vercel will ignore them.

4. VERIFY
   After Vercel finishes deploying, open:

       /api/health

   It MUST show:

       "version":"1.11.13"
       "build":"drop-in-no-wipe"

If those values appear, the deployment problem is fixed.

FUTURE UPDATES
You can keep uploading future update contents to the GitHub ROOT.
No special trenches-app Root Directory is needed.

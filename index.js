import http from 'node:http';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';
const __dirname=path.dirname(fileURLToPath(import.meta.url));const root=path.join(__dirname,'..','public');const PORT=Number(process.env.PORT||8787);const RPC=process.env.SOLANA_RPC_URL||'https://api.mainnet-beta.solana.com';
async function rpc(method,params){const r=await fetch(RPC,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({jsonrpc:'2.0',id:1,method,params})});if(!r.ok)throw new Error(`RPC ${r.status}`);const j=await r.json();if(j.error)throw new Error(j.error.message||'RPC error');return j.result}
const metric=(value,status,detail)=>({value,status,detail});
function scoring(x){let score=50;if(x.mintAuthority===null)score+=13;else score-=14;if(x.freezeAuthority===null)score+=12;else score-=12;if(x.top10Pct!=null){if(x.top10Pct<20)score+=13;else if(x.top10Pct<35)score+=5;else if(x.top10Pct>60)score-=20;else score-=8}if(x.supply>0)score+=5;score=Math.max(0,Math.min(100,Math.round(score)));return{score,label:score>=80?'LOOKS HEALTHY':score>=55?'BE CAREFUL':'HIGH RISK',tone:score>=80?'good':score>=55?'warn':'bad'}}
function json(res,code,obj){res.writeHead(code,{'content-type':'application/json','access-control-allow-origin':'*'});res.end(JSON.stringify(obj))}
async function scanMint(mint){const [info,supply,largest]=await Promise.all([rpc('getAccountInfo',[mint,{encoding:'jsonParsed',commitment:'confirmed'}]),rpc('getTokenSupply',[mint,{commitment:'confirmed'}]),rpc('getTokenLargestAccounts',[mint,{commitment:'confirmed'}])]);const parsed=info?.value?.data?.parsed?.info;if(!parsed)throw Object.assign(new Error('Mint account was not found or is not a standard parsed SPL token mint.'),{code:404});const rawSupply=Number(supply?.value?.uiAmountString||supply?.value?.uiAmount||0),vals=(largest?.value||[]).map(x=>Number(x.uiAmountString||x.uiAmount||0)),top10Amt=vals.slice(0,10).reduce((a,b)=>a+b,0),top10Pct=rawSupply>0?top10Amt/rawSupply*100:null,s=scoring({mintAuthority:parsed.mintAuthority??null,freezeAuthority:parsed.freezeAuthority??null,top10Pct,supply:rawSupply}),topStatus=top10Pct==null?'unknown':top10Pct<20?'good':top10Pct<35?'warn':'bad';return{mint,name:'On-chain token',symbol:'TOKEN',verified:false,...s,summary:`Salt found ${s.label==='LOOKS HEALTHY'?'no major issues in the checks available in V1':s.label==='BE CAREFUL'?'some risk factors worth reviewing before you trade':'serious warning signs in the checks available in V1'}. Missing data is shown as unknown instead of guessed.`,authenticity:metric('Unverified','unknown','The mint exists on Solana, but official-project identity verification still needs project/social evidence.'),sellable:metric(process.env.JUPITER_API_KEY?'Ready to quote':'Needs Jupiter key','unknown','Live sell-route checks use the Jupiter swap integration.'),mintAuthority:metric(parsed.mintAuthority?'Active':'Disabled',parsed.mintAuthority?'bad':'good',parsed.mintAuthority?'A mint authority can still create additional supply.':'No mint authority is currently set.'),freezeAuthority:metric(parsed.freezeAuthority?'Active':'Disabled',parsed.freezeAuthority?'bad':'good',parsed.freezeAuthority?'A freeze authority is still present.':'No freeze authority is currently set.'),top10:metric(top10Pct==null?'Unknown':`${top10Pct.toFixed(1)}%`,topStatus,top10Pct==null?'Could not calculate concentration.':`The 10 largest token accounts hold about ${top10Pct.toFixed(1)}% of current supply. V1 has not yet classified LP/exchange/burn accounts, so this is a raw signal.`),owner:metric('Data source needed','unknown','Creator-linked holdings require wallet graph analysis; Salt refuses to invent this value.'),bundled:metric('Data source needed','unknown','Bundle detection requires transaction and wallet-cluster data.'),snipers:metric('Data source needed','unknown','Launch-time transaction history is needed for accurate sniper detection.'),liquidity:metric('Route data needed','unknown','Liquidity and price impact will come from the routing integration.'),holders:metric('Indexer needed','unknown','Exact holder count requires indexed token-account data.'),duplicates:metric('Verification engine needed','unknown','Duplicate detection will compare metadata, socials, launch source and competing mint addresses.'),creatorHistory:metric('Graph data needed','unknown','Deployer history requires indexed transaction history and wallet attribution.')}}
const server=http.createServer(async(req,res)=>{try{const u=new URL(req.url,'http://localhost');if(u.pathname==='/api/health')return json(res,200,{ok:true,service:'Salt Swap V1'});if(u.pathname.startsWith('/api/scan/')){const mint=decodeURIComponent(u.pathname.slice('/api/scan/'.length)).trim();if(!/^[1-9A-HJ-NP-Za-km-z]{32,44}$/.test(mint))return json(res,400,{error:'That does not look like a valid Solana mint address.'});try{return json(res,200,await scanMint(mint))}catch(e){return json(res,e.code||500,{error:`Scan failed: ${e.message}`})}}if(u.pathname==='/api/jupiter/order'){if(!process.env.JUPITER_API_KEY)return json(res,503,{error:'JUPITER_API_KEY is not configured yet.'});const qs=u.searchParams.toString();const r=await fetch(`https://api.jup.ag/swap/v2/order?${qs}`,{headers:{'x-api-key':process.env.JUPITER_API_KEY}});res.writeHead(r.status,{'content-type':'application/json'});return res.end(await r.text())}const p=u.pathname==='/'?'/index.html':u.pathname;const full=path.normalize(path.join(root,p));if(!full.startsWith(root)||!fs.existsSync(full))return json(res,404,{error:'Not found'});const ext=path.extname(full),types={'.html':'text/html','.css':'text/css','.js':'text/javascript'};res.writeHead(200,{'content-type':types[ext]||'application/octet-stream'});fs.createReadStream(full).pipe(res)}catch(e){json(res,500,{error:e.message})}});
server.listen(PORT,()=>console.log(`🧂 Salt Swap V1 running at http://localhost:${PORT}`));
